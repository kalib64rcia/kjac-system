import { PageHeader } from "@/components/shared/PageHeader";

/** Sprint 1 placeholder — dashboard ships in Sprint 6. */
export function DashboardPage() {
  return (
    <PageHeader
      title="Dashboard"
      description="KPIs, charts, and widgets ship in Sprint 6."
    />
  );
}
