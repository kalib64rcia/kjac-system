// DEPRECATED: Old SchedulePage using TimeSlotSchedule (custom grid).
// Replaced by FullCalendar (Monarch shadcn theme) implementation.
// Kept for reference during migration to FullCalendar.
//
// NOTE: body removed; the live page is SchedulePage.tsx. This stub is
// intentionally import-free so `tsc --noEmit` stays green.
export {};
